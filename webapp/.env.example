POSTGRES_USER=dahlia
POSTGRES_PASSWORD=dahlia
POSTGRES_DB=dahlia
POSTGRES_PORT=5432
POSTGRES_HOST=localhost # dahlia-webapp-db inside the docker-compose file

# URL de connexion utilisée par Prisma
# (construite à partir des variables ci-dessus)
DATABASE_URL="postgresql://dahlia:dahlia@localhost:5432/dahlia?schema=public"
